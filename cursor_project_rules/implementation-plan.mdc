# Implementation Plan Progress

1. scaffold - Done
Done - Created monorepo root structure, base directories, and environment template.
Done - Added startup/shutdown scripts and baseline README for MVP workflow.

2. kratos-services - Done
Done - Created five service modules under app/ with Kratos runtime entrypoints.
Done - Wired dedicated service names and ports for gateway/auth/attendance/voice/admin.

3. proto-sqlite - Done
Done - Added API protobuf contracts for auth, attendance, voice, and admin service boundaries.
Done - SQLite DDL + seeds live in app/dbschema/bootstrap.sql; data/schema.sql points there (see data/DATA_SYNC.md).

4. voice-svc - Done
Done - Implemented /v1/voice/recognize with multipart audio handling and structured JSON response.
Done - Added OpenSpeech ASR + Doubao NLU calls with deterministic fallback when credentials or upstream fail.

5. voice-test - Done
Done - Added synthetic sample audio and voice endpoint smoke script under scripts/test-voice.sh.
Done - Script posts multipart wav and pretty-prints JSON response for recognition verification.

6. auth-attendance-admin - Done
Done - Implemented login/wechat mock, attendance submit+today query, and admin board aggregation APIs.
Done - Wired SQLite-backed persistence with schema bootstrap and seeded demo accounts.

7. gateway - Done
Done - Added gateway route fan-out, CORS handling, and JWT authorization guard for protected routes.
Done - Rebased gateway to port 8010 to avoid system port conflict on 8000.

8. frontend - Done
Done - Built Vue3+Vite+Vant H5 screens: Login, EmployeeHome with VoiceRecorder, AdminDashboard.
Done - Applied Professional-style tokens and integrated API calls for full MVP path.

9. e2e-smoke - Done
Done - Started full local stack and validated login -> voice recognize -> submit -> admin board flow via gateway.
Done - Confirmed frontend URL and gateway URL are available and runnable in current environment.

10. home-work-report - Done
Done - HomePage.vue: two hero cards (工作申报 / 一键打卡下班), van-popup form with calendar/tags/submit, CHECK_OUT in modal after save.
Done - Gating via hasReportedToday + showConfirmDialog「去申报」; submitStatus + loadRecords; npm run build verified.

11. home-work-report-multi - Done
Done - Work report modal: multiple lines via「新增一条」, compact textarea per line, per-line tag; confirm dialog before submit on main itinerary coverage.
Done - Submit loops submitStatus with bumped occurredAt seconds; build verified.

12. api-base-admin-users - Done
Done - Frontend axios base URL defaults to same-origin (empty) so nginx `/v1` proxy works in prod; Vite dev proxies `/v1` to gateway; optional `VITE_API_BASE_URL`.
Done - Auth JWT `sub` parsing accepts float64/string/json.Number; admin account list errors distinguish 401/403/network + retry.

13. flows-audit - Done
Done - Fixed TeamMemberHistoryView list key (`records` not `items`); derived 在岗天数 from timeline; try/catch + toasts on Team/Home/Schedule/MyAttendance/Report/Profile; MyAttendance reload on period change.
Done - Attendance submit validates allowed status set server-side; builds verified.

14. ux-nav-loading - Done
Done - AppBottomNav derives active tab from route (`/my-attendance` → 行程); safe-area + blur bar; `--app-nav-clearance` for content padding; router scroll to top; login loading + Home submit/clock-out loading.
Done - Login gradient + dvh; DetailedAttendanceReport bottom clearance; zh-CN document title.

15. schedule-page-compact-ssot - Done
Done - SchedulePage: merged「当日上班时间」into timeline header beside actions; summary as compact chips; tighter spacing; removed localStorage schedule events.
Done - Timeline labels + save path use only `attendance_records` via submitStatus(OFFICE)+getAttendanceByDate; slot text maps `occurredAt` to 30m buckets; `npm run build` OK.

16. db-single-schema-sync-prep - Done
Done - Added intervoice/dbschema with embedded bootstrap.sql (sync_uid, updated_at, FK on attendance, seed_markers); auth/attendance/admin call ApplySQLite + WAL/foreign_keys/busy_timeout DSN; MigrateSQLiteLegacy for older DB files.
Done - postgresql.sql mirror + data/DATA_SYNC.md (single writer vs true 2-way, replication checklist); data/schema.sql reduced to pointers only.

17. admin-data-api-mcp - Done
Done - Admin-only JWT routes /v1/admin/data/meta|schema|users|attendance + POST attendance/upsert + users/patch; 1MiB body cap, optimistic ifMatchUpdatedAt, admin_data_audit; gateway proxies all.
Done - scripts/mcp-intervoice-admin (MCP stdio + Zod tools); cursor-mcp-config.example.json; DATA_SYNC.md admin+MCP section; admin+gateway go build OK.

18. sqlite-migrate-legacy-fix - Done
Done - MigrateSQLiteLegacy: avoid ALTER ADD with non-constant default (SQLite error on older DB); nullable columns + backfill updated_at + partial UNIQUE indexes on sync_uid.
Done - dev-up smoke: employee/admin login, bottom nav, team guard, work申报→submit→today API record OK.

19. e2e-flow-smoke - Done
Done - Browser MCP + curl: /healthz, auth login, employee paths /home /schedule /me /my-attendance/day, non-admin blocked from /team-attendance, admin team view loads.
Done - Work declaration modal saves OFFICE row for admin userId=900001; clock-out card text updates after申报.

20. mgm-location-catalog-ui - Done
Done - Embedded `location_catalog.sql`, `ApplySQLite` + `GET /v1/catalog/locations` (attendance) + gateway proxy; HomePage two office presets + dining picker with intro popup; SchedulePage matching quick presets + `CatalogIntroPopup`; PostgreSQL DDL mirror for `location_catalog`.
Done - `npm run build` and Go build for attendance/gateway/dbschema verified.

21. schedule-quick-admin-config - Done
Done - `schedule_quick_section` SQL + seeds; `GET /v1/catalog/schedule-quick` (gateway 免登录); Schedule modal driven by grouped catalog only (no hotel intro buttons); admin CRUD `/v1/admin/data/location-catalog*` + `schedule-quick-sections*` + `AdminScheduleQuickConfigView.vue` (`/me/schedule-quick-config`); `schedule_chip` seeded for 客户拜访/会议/文档整理。
Done - `npm run build` + Go admin/attendance/gateway build verified。

22. admin-routes-restart-doc-metro-home - Done
Done - 404 root cause: stale `admin`/`gateway` process without new routes; fixed by `dev-down`/`dev-up`; `dev-up.sh` + README reminder; admin config page 404 toast text; HomePage Metro tiles: 一键打卡上班、快捷申报、一键快乐下班、今日好运 + horizontal 「实况」 cards。
Done - `npm run build` OK。

23. zh-admin-quick-theme-global - Done
Done - `style.css` expanded tokens + `--van-*` linkage; Home metro + bottom nav use tokens; 行程快捷配置：中文主导、ActionSheet 中文主标题、区域中文映射、一级分组下二级预览芯片（全量目录快照）；`getAdminLocationCatalog()` 无参拉全表。
Done - `npm run build` OK。

24. schedule-shift-ux-dev-up-admin - Done
Done - `scripts/dev-up.sh`: list ports、仅清理本仓库占用进程、占用仍存则退出、`curl` 等待 gateway `/healthz`；行程时间轴右侧药丸展示 `常规班-{名称} {时段}`；`DailySchedulePicker` 改为常规班/休假两段下拉（Picker）；`GET` pillText 与休假前缀文案对齐；管理员页 `/me/schedule-types-config` 维护 `shift_types`/`activity_types`；`npm run build` + `go build` attendance OK。
Done - 数据库仍由 `dbschema` 引导；OFFICE 与迁移逻辑不变。

25. rewards-leads-i18n-home - Done
Done - `dbschema` 增加 `rewards.sql`（badge_definitions / user_streaks / user_badges）与 `leads.sql`（client_leads / events + DEMO 种子）；attendance：`processRewardsAfterSubmit` 在 CHECK_IN/CHECK_OUT 后更新连击并返回 `newlyEarnedBadges`；`/v1/rewards/me`、`/v1/rewards/ack`、`/v1/leads/*`；admin `GET /v1/admin/leads`；gateway 代理；前端 `vue-i18n` + `locales/zh-CN|en`、`BadgeCelebration`（confetti）、首页线索磁贴、`LeadFeedView`/`LeadDetailView`、`MyBadgesView`、`AdminLeadsView`、我的页语言 ActionSheet。
Done - `npm run build` 与 `go build`（auth/attendance/admin/gateway）通过。

26. rewards-leads-docs-ux-polish - Done
Done - `.gitignore` 增加 `*.db-shm`/`*.db-wal`；`DATA_SYNC.md` 与 `README.md` 补充 rewards/leads 表说明与网关路由；`postgresql.sql` 对 `badge_definitions` 增加与 SQLite 一致的 `ON CONFLICT DO NOTHING` 种子；`LeadDetailView` 仅在 `assignedUserId`/`pickedUpBy` 与当前用户一致时显示跟进表单，否则展示 i18n 提示。
Done - `npm run build` 通过。

27. sqlite-integrity-schema-guards - Done
Done - `dbschema.VerifySQLitePostApply`：`PRAGMA foreign_key_check` + `PRAGMA quick_check`；`MigrateSQLiteLegacy` 末尾 `migrateRewardsLeadsReferentialSanity` 清理孤儿行并钳位 streak；`ApplySQLite` 在迁移后执行校验；PostgreSQL DDL 为 `badge_definitions`/`user_streaks`/`client_leads` 增加 CHECK；线索 `follow-up` 校验 `statusTo` 枚举；`app/attendance/schema_verify_test.go` 内存库冒烟。
Done - `go test ./...`（attendance 模块）与 `go build` dbschema 消费方通过。

28. macau-leads-editorial-ui - Done
Done - `client_leads` 扩展（纯新客/老客激活、来源地、MGM 场域、渠道、参考字段）+ `migrateClientLeadColumns`；`dbschema.LeadWideSelectColumns` 与 `ScanClientLeadWideRow` 共用；attendance `/v1/leads/*` 与 admin `/v1/admin/leads` 输出新字段；H5 `style.css`/`index.html` 字体与「Macau IR editorial」色板，`HomePage`/`Lead*`/`AdminLeadsView` 与 i18n 展示。
Done - `go test`（attendance）、`npm run build` 通过。

29. leads-value-radar-visual - Done
Done - `frontend/src/lib/leadValuePotential.js`：六维 0–100 规则算法 + 加权潜力指数；`LeadValueRadarChart.vue` SVG 雷达；`LeadFeedView` 卡片化列表 + 迷你雷达与潜力分；`LeadDetailView` 雷达 + 条形维度分解；首页线索磁贴展示潜力芯片；`zh-CN`/`en`/`README` 说明。
Done - `npm run build` 通过。

30. fortune-calendar-schedule-grid-io - Done
Done - `user_daily_fortune` + attendance `/v1/fortune/day|month`、静态 `/uploads/fortune/`；admin 上传/assign/sync；`GET /v1/schedule/month`；admin `schedule/grid/export|import`（含 Chronoscape `20260517.json` 对齐）；gateway 代理与 multipart 转发；前端 `FortuneCalendarPopup`、`AdminEmployeeFortuneView`（`/me/accounts/:userId/fortune`）、行程月历班次 `bottomInfo`、团队考勤班次 JSON 导入导出。
Done - `go build`（attendance/admin/gateway/dbschema）与 `npm run build` 通过。
